package com.doublez.backend.enums;

public enum CreditPackageType {
    TIER_SUBSCRIPTION,  // Agency tier monthly subscriptions
    FEATURE,            // One-time features (featured listings, etc.)
    ONE_TIME,           // Single purchases (project postings, etc.)
    PROMOTIONAL         // Free credits (bonuses, trials, promotions)
}
